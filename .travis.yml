language: node_js
node_js:
  - stable
addons:
  sauce_connect: true
env:
  - CXX=g++-4.8
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-4.8
after_success:
  - cat ./coverage/lcov/lcov.info | ./node_modules/coveralls/bin/coveralls.js
